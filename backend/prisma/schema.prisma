generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  nome      String
  perfil    String   @default("usuario") // "admin", "advogado", "usuario"
  ativo     Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  processos Processo[]
  clientes  Cliente[]
}

model Cliente {
  id           String   @id @default(cuid())
  nome         String
  cpfCnpj      String   @unique
  email        String?
  telefone     String?
  endereco     String?
  cidade       String?
  estado       String?
  cep          String?
  observacoes  String?
  ativo        Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  userId   String
  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  processos Processo[]
}

model Processo {
  id          String   @id @default(cuid())
  titulo      String
  area        String
  prazo       DateTime
  status      String   @default("ativo") // "ativo", "aguardando", "concluído", "suspenso"
  descricao   String?
  prioridade  String   @default("normal") // "baixa", "normal", "alta"
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  userId   String
  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  clienteId String
  cliente   Cliente  @relation(fields: [clienteId], references: [id], onDelete: Cascade)
}
